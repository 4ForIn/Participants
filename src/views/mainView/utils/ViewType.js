export const viewType = (type) => (type ? 'authView' : 'main');

/**
 * fn that recives a boolean
 * and returns string (that is a property of the object returned by the mainViewSwitchFn function)
 */
